# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.pytest_cache/

# Environment & secrets
.env
*.env
credentials.json
hydra_ws_token.json
dashboard/public/hydra_ws_token.json
hydra_auth_state.json
hydra_auth_state.json.tmp

# Node
node_modules/
dashboard/node_modules/
dashboard/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Logs & runtime
*.log
*.db
*.sqlite
*.sqlite3
hydra_order_journal.json
hydra_order_journal.json.tmp
hydra_order_journal.json.harness_stash
hydra_trades_*.json
hydra_trades_live.json
hydra_trades_live.json.harness_stash
hydra_trades_live.json.migrated
competition_results_*.json
hydra_params_*.json
hydra_session_snapshot.json
hydra_session_snapshot.json.tmp
hydra_session_snapshot.json.harness_stash

# Claude Code — per-user / runtime files only.
# .claude/settings.json, .claude/skills/, and .claude/hooks/ are committed.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local agent memory (project-scoped, not tracked)
HYDRA_MEMORY.md
.hydra-memory/

# Backtest & experimentation artifacts (v2.10.0+; runtime, not tracked)
.hydra-experiments/
hydra_backtest_errors.log

# Brain audit log (v2.14.0+; JSONL of every deliberate() and fallback)
hydra_brain.jsonl

# Companion runtime artifacts (transcripts, memory, proposals, routing/cost logs)
.hydra-companions/

# Thesis layer (v2.13.0+; runtime state + user-uploaded research, not tracked)
hydra_thesis.json
hydra_thesis.json.tmp
hydra_thesis_documents/
hydra_thesis_processed/
hydra_thesis_pending/
hydra_thesis_evidence_archive/

# Temporary / Junk / Scratch
*.diff
*.patch
AUDIT_*.md
test_ws.*
fix_host.py

# Canonical historical OHLC store (rebuild via tools/bootstrap_history.py)
hydra_history.sqlite
hydra_history.sqlite-shm
hydra_history.sqlite-wal

# Kraken personal trade history store + raw dumps (rebuild via tools/sync_kraken_trades.py)
hydra_kraken_trades.sqlite
hydra_kraken_trades.sqlite-shm
hydra_kraken_trades.sqlite-wal
kraken_trades_dump.json

# Journal backfill backups (one per --apply run; safe to delete after verification)
hydra_order_journal.json.backup-*
