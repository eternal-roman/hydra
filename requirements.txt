# Runtime + CI pins for HYDRA (stdlib-first engine; these are agent/dashboard deps).
# bcrypt<5.0 — passlib 1.7.4 is unmaintained and DIES on bcrypt 5.x: its
# detect_wrap_bug() probe at backend init raises "ValueError: password cannot
# be longer than 72 bytes", so the first CryptContext hash/verify fails and
# auth is dead. The <5.0 ceiling is LOAD-BEARING — do not raise it without
# replacing passlib. (On bcrypt 4.x passlib also logs a trapped
# "error reading bcrypt version" from the removed __about__ attribute; that
# one is harmless and is NOT the reason for the pin.)
anthropic>=0.121.0,<1.0
openai>=2.54.0,<3.0
pytest>=9.1.1
PyJWT>=2.13.0
cryptography>=50.0.0
passlib[bcrypt]>=1.7.4
bcrypt<5.0
websockets>=16.1.1
# heartbeat/src/heartbeat/config.py imports yaml. Absent from this file until
# v2.32, so `pytest tests/` red-lined on test_heartbeat_surface.py the moment
# the catch-all CI step existed to run it.
PyYAML>=6.0.3
